import queue from './queue';
import * as home from './home/home';
import * as categorySettings from './settings/category';

const actions = {
    home,
    queue,
    categorySettings,
};

export default actions;
